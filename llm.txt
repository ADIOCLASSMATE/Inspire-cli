# Inspire CLI - AI Notes

> Quick reference for AI assistants working with the Inspire CLI codebase.

## Project Overview

Inspire CLI is a command-line tool for the Inspire HPC platform:
- Submit and manage jobs on GPU clusters
- Monitor status and stream logs
- Launch and interact with notebook sessions (terminal & exec)
- Inspect GPU/node availability

## Public Sync Scope (.gitignore)

Treat `.gitignore` as the source of truth for public-sync boundaries.

Ignored/internal paths currently include:
- `.inspire/`
- `internal/`
- `scripts/`
- `API_ENDPOINTS.md`
- `CLAUDE.md`
- `config.toml.example`

When preparing `github-public` sync, avoid adding docs/tests/examples that depend on ignored files.

## Architecture (Current)

```text
inspire-cli/
├── inspire/
│   ├── __init__.py
│   ├── cli/
│   │   ├── main.py
│   │   ├── context.py
│   │   ├── commands/
│   │   │   ├── config/
│   │   │   ├── init/
│   │   │   ├── job/
│   │   │   ├── notebook/
│   │   │   ├── project/
│   │   │   ├── resources/
│   │   │   └── run.py
│   │   ├── formatters/
│   │   └── utils/
│   ├── config/                  # Config models/loaders/schema/options
│   ├── platform/
│   │   ├── openapi/             # Token-based API client + resource matching
│   │   └── web/                 # SSO session + browser-only APIs
│   │       ├── session/
│   │       └── browser_api/
│   │           └── terminal_helpers.py  # Jupyter terminal REST + WebSocket ops
│   └── core/
├── tests/
├── AGENTS.md
└── pyproject.toml
```

## Top-Level CLI Groups

```bash
inspire job ...
inspire resources ...
inspire config ...
inspire run ...
inspire notebook ...
inspire image ...
inspire project ...
inspire init
```

## Notebook Access Modes

- `inspire notebook terminal <id>` — interactive terminal via Jupyter WebSocket
- `inspire notebook exec <id> "<cmd>"` — non-interactive command execution

SSH/tunnel/bridge/sync commands have been removed.

## Configuration Model

### Precedence
```text
Defaults < Global TOML < Project TOML < Environment variables
```

### Config File Locations
- Global: `~/.config/inspire/config.toml`
- Project: `./.inspire/config.toml`

### High-Impact Env Vars
- `INSPIRE_USERNAME`
- `INSPIRE_PASSWORD`
- `INSPIRE_TARGET_DIR`
- `INSPIRE_BASE_URL`
- `INSPIRE_SHM_SIZE`

## Common Development Commands

```bash
uv tool install -e .
uv run pytest
uv run pytest -m integration
uv run ruff check .
uv tool run black .
inspire --help
```

## Code Patterns

### Load Config
```python
from inspire.config import Config

cfg, sources = Config.from_files_and_env(require_credentials=True)
```

### Get Authenticated API Client
```python
from inspire.cli.utils.auth import AuthManager

api = AuthManager.get_api(cfg)
```

### Click Command Pattern
```python
import click
from inspire.cli.context import Context, pass_context

@click.command()
@pass_context
def my_command(ctx: Context) -> None:
    if ctx.json_output:
        click.echo('{"ok": true}')
    else:
        click.echo("ok")
```

## Exit Codes

| Code | Meaning |
|------|---------|
| 0 | Success |
| 10 | Configuration error |
| 11 | Authentication failed |
| 12 | Validation error |
| 13 | API error |
| 14 | Timeout |

## Where To Edit

1. Add a config option:
   - `inspire/config/options/`
   - `inspire/config/schema.py` / `schema_models.py`
   - `inspire/config/models.py`
2. Add a command:
   - Command registry under `inspire/cli/commands/<group>/__init__.py`
   - Implementation in `inspire/cli/commands/<group>/...`
3. Add OpenAPI behavior:
   - `inspire/platform/openapi/`
4. Add browser-only notebook/resource behavior:
   - `inspire/platform/web/browser_api/`
5. Add tests:
   - `tests/test_*.py`

## Notes

- Keep reusable logic out of CLI-only `_impl` paths when possible.
- Prefer `inspire/config` and `inspire/platform` for shared logic.
- For public syncs, double-check changes against `.gitignore` before pushing.
